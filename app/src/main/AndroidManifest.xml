<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MidtermRachadSouaiby"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MidtermRachadSouaiby">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Spending Analysis Activity -->
        <activity
            android:name=".SpendingAnalysisActivity"
            android:exported="true"
            android:label="@string/spending_analysis_label"
            android:theme="@style/Theme.MidtermRachadSouaiby" />

        <!-- Transaction History Activity -->
        <activity
            android:name=".TransactionHistoryActivity"
            android:exported="true"
            android:label="@string/transaction_history_label"
            android:theme="@style/Theme.MidtermRachadSouaiby" />

    </application>
</manifest>
